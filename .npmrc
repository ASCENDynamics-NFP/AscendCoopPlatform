# This project intentionally uses @angular/fire@20 alongside Angular 21.
# The peer-dependency mismatch is known and does not affect runtime behaviour
# (AngularFire compat APIs remain stable across this version range).
# legacy-peer-deps prevents npm install from failing on this conflict.
legacy-peer-deps=true
