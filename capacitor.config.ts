import {CapacitorConfig} from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "io.ionic.starter",
  appName: "ascendcoopplatform",
  webDir: "public",
  server: {
    androidScheme: "https",
  },
};

export default config;
